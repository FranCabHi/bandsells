Thank you for your purchase, <%= @user.name %>!

Order Number: <%= @order.id %>
Payment date: <%= @payment.created_at.strftime("%B %d, %Y") %>
Order Details:
<% @order.products.each do |item| %>
  <%= item.get_quantity(@order) %>
  <%= link_to item.title, item %>
  <%= number_to_currency(item.unit_price, strip_insignificant_zeros: true, delimiter: ".") %>
  ============
<% end %>

Your order is just a step away!

