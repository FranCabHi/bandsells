<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Thank you for your purchase, <%= @user.name %>!</h1>
    <p>Order Number: <%= @payment.order.id %></p>
    <p>Payment date: <%= @payment.created_at.strftime("%B %d, %Y") %></p>
    <table class="table">
      <thead>
        <tr>
          <th scope="col">Product</th>
          <th scope="col">Unit price</th>
        </tr>
      </thead>
      <tbody>
        <% @payment.order.products.each do |item| %>
          <tr>
            <td><strong><%= item.title %></strong></td>
            <td><%= number_to_currency(item.unit_price, strip_insignificant_zeros: true, delimiter: ".") %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <p>Total: <%= number_to_currency(@payment.total, strip_insignificant_zeros: true, delimiter: ".") %></p>
    <p>Your order is just a step away!</p>
  </body>
</html>